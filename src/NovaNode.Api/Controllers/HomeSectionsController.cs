// HomeSections feature has been deprecated.
// The storefront now uses a fixed premium layout driven by existing APIs.
// DB tables are kept for migration safety but this controller is no longer active.

namespace NovaNode.Api.Controllers;

// Controller removed — HomeSections feature deprecated.
