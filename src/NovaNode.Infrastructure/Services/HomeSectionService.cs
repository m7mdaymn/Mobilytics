// HomeSectionService has been deprecated.
// The storefront now uses a fixed premium layout driven by existing APIs.
// DB tables are kept for migration safety but this service is no longer active.

namespace NovaNode.Infrastructure.Services;

// Service removed — HomeSections feature deprecated.
